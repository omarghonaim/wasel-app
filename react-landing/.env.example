# Leave empty for same-origin requests (recommended for wasel.com and local :8000).
# Dev on :5173 uses Vite proxy in vite.config.ts for /api.
VITE_API_BASE_URL=

# Config endpoint path used for logo / business settings.
# Change only this when the backend route changes.
VITE_CONFIG_ENDPOINT=/api/v1/config
